preco = float(input("Digite o valor que queira calcular: "))
desconto = float(input("Digite o valor de desconto que queira calcular: "))

valor_desconto = preco * (desconto/100)

total = preco - valor_desconto

#print(total)

a = "ab" * 3
print(a)
print("ab" * 3)